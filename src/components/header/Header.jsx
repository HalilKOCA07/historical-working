import Container from "react-bootstrap/Container"
import headerStyle from "./header.module.scss"

const Header = () => {
    
    return(
        <header>
            <h1>HISTORICAL AREA</h1>
        </header>
    )
}

export default Header